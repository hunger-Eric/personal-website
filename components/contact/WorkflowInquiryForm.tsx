"use client";
import { FormEvent,useState } from "react";
import { useSearchParams } from "next/navigation";
import { ArrowRight,CheckCircle2 } from "lucide-react";
import { useLocale } from "@/components/LocaleProvider";
import { getOpenGeoContactContext } from "@/config/open-geo-demo";
type FormStatus="idle"|"sending"|"sent"|"error";
export function WorkflowInquiryForm(){const{locale}=useLocale();const zh=locale==="zh";const params=useSearchParams();const context=getOpenGeoContactContext({project:params.get("project"),scenario:params.get("scenario"),artifact:params.get("artifact")},locale);const[status,setStatus]=useState<FormStatus>("idle");const[message,setMessage]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setStatus("sending");setMessage("");const form=event.currentTarget;try{const response=await fetch("/api/contact",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(form).entries()))});const result=(await response.json()) as {message?:string};if(!response.ok)throw new Error(result.message||"Request failed");form.reset();setStatus("sent")}catch(error){setStatus("error");setMessage(error instanceof Error?error.message:zh?"提交失败，请稍后重试。":"Submission failed. Please try again.")}}if(status==="sent")return <div role="status" className="border border-accent bg-surface-paper-elevated p-6"><CheckCircle2 className="h-7 w-7 text-accent" aria-hidden/><h2 className="mt-5 text-2xl font-semibold text-foreground">{zh?"问题已提交":"Your workflow was submitted"}</h2><p className="mt-3 text-sm leading-7 text-muted-foreground">{zh?"我会人工查看你提供的信息与模拟体验上下文，再确认是否适合进一步讨论。":"I will review your information and simulation context before confirming any next discussion."}</p></div>;const input="mt-2 w-full border border-hairline bg-surface-paper-elevated px-3 py-3 text-sm text-foreground outline-none transition focus:border-accent focus:ring-1 focus:ring-accent";return <form onSubmit={submit} className="space-y-5" aria-label={zh?"业务问题提交表单":"Business inquiry form"}>{context?<section className="border border-accent bg-surface-paper p-4" aria-label={zh?"模拟体验上下文":"Simulation context"}><p className="font-mono text-[10px] uppercase tracking-[0.14em] text-accent">{zh?"来自模拟体验 · 模拟数据":"From simulated experience · simulated data"}</p><h2 className="mt-2 font-semibold text-foreground">{context.projectName}</h2><dl className="mt-3 grid gap-2 text-sm"><div><dt className="text-muted-foreground">{zh?"场景":"Scenario"}</dt><dd className="font-medium text-foreground">{context.scenarioTitle}</dd></div><div><dt className="text-muted-foreground">{zh?"模拟交付物":"Simulated artifact"}</dt><dd className="font-medium text-foreground">{context.artifactTitle}</dd></div></dl><input type="hidden" name="project" value={context.projectId}/><input type="hidden" name="scenario" value={context.scenarioId}/><input type="hidden" name="artifact" value={context.artifactId}/></section>:null}<div className="grid gap-5 sm:grid-cols-2"><label className="text-sm font-semibold text-foreground">{zh?"你的姓名":"Your name"}<input name="name" required maxLength={80} autoComplete="name" className={input}/></label><label className="text-sm font-semibold text-foreground">{zh?"工作邮箱":"Work email"}<input name="email" required type="email" maxLength={160} autoComplete="email" className={input}/></label></div><label className="block text-sm font-semibold text-foreground">{zh?"公司与业务场景":"Company and business context"}<input name="company" required maxLength={160} className={input}/></label><label className="block text-sm font-semibold text-foreground">{zh?"目前的流程是怎样的？":"How does the workflow work today?"}<textarea name="workflow" required minLength={30} maxLength={3000} rows={6} className={input}/></label><div className="grid gap-5 sm:grid-cols-2"><label className="text-sm font-semibold text-foreground">{zh?"发生频率":"Frequency"}<select name="frequency" required className={input} defaultValue=""><option value="" disabled>{zh?"请选择":"Select"}</option><option value="daily">{zh?"每天":"Daily"}</option><option value="weekly">{zh?"每周":"Weekly"}</option><option value="monthly">{zh?"每月":"Monthly"}</option><option value="event">{zh?"按业务事件触发":"Event-driven"}</option></select></label><label className="text-sm font-semibold text-foreground">{zh?"每次大约投入多少人工时间？":"Approximate manual time per run"}<input name="manualTime" required maxLength={120} className={input}/></label></div><label className="sr-only" aria-hidden="true">Website<input name="website" tabIndex={-1} autoComplete="off"/></label>{status==="error"?<p role="alert" className="text-sm font-medium text-destructive">{message}</p>:null}<button type="submit" disabled={status==="sending"} className="inline-flex min-h-11 w-full items-center justify-center gap-2 bg-accent px-5 py-3.5 text-sm font-semibold text-accent-foreground hover:bg-accent-hover disabled:opacity-60">{status==="sending"?(zh?"正在提交…":"Submitting…"):(zh?"提交并申请初步诊断":"Submit for initial diagnosis")}<ArrowRight className="h-4 w-4" aria-hidden/></button><p className="text-xs leading-5 text-muted-foreground">{zh?"提交不等于自动报价或保证安排会议。你的信息只用于判断是否适合进一步沟通。":"Submission is not an automatic quote or guaranteed meeting. Your information is used only to assess fit."}</p></form>}
