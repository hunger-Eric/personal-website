"use client";
import Link from "next/link";
import { useLocale } from "@/components/LocaleProvider";
export function Footer() { const { locale } = useLocale(); const zh = locale === "zh"; const subtitle = zh ? "企业 AI 系统设计与交付" : "Enterprise AI system design and delivery"; return <footer className="border-t border-hairline bg-surface-paper text-surface-paper-foreground"><div className="mx-auto grid max-w-6xl gap-8 px-4 py-10 sm:grid-cols-[1fr_auto] sm:items-end"><div><p className="text-lg font-semibold tracking-tight text-foreground">实解智能</p><p className="mt-1 text-sm text-muted-foreground">{subtitle}</p><p className="mt-2 max-w-xl text-sm leading-6 text-muted-foreground">{zh ? "让 AI 真正在企业里跑起来。" : "Make AI truly run in your business."}</p></div><nav aria-label={zh ? "页脚导航" : "Footer navigation"} className="flex flex-wrap gap-x-5 gap-y-2 text-sm font-medium text-muted-foreground"><Link href="/services">{zh ? "服务" : "Services"}</Link><Link href="/projects">{zh ? "项目库" : "Projects"}</Link><Link href="/articles">{zh ? "文章" : "Articles"}</Link><Link href="/articles#wechat">{zh ? "公众号" : "WeChat"}</Link><Link href="/contact">{zh ? "联系" : "Contact"}</Link></nav></div><div className="border-t border-hairline px-4 py-4 text-center font-mono text-[11px] uppercase tracking-[0.14em] text-muted-foreground">© {new Date().getFullYear()} 实解智能 · {subtitle}</div></footer>; }
