"use client";
import Link from "next/link";
import { useLocale } from "@/components/LocaleProvider";
import { localizePublicPath } from "@/config/locale";
import { publicIdentity } from "@/config/public-identity";
export function Footer() { const { locale } = useLocale(); const zh = locale === "zh"; const subtitle = publicIdentity.category[locale]; const brandName=publicIdentity.names[locale]; const path=(value:string)=>localizePublicPath(value,locale); return <footer className="border-t border-hairline bg-surface-paper text-surface-paper-foreground"><div className="mx-auto grid max-w-6xl gap-8 px-4 py-10 sm:grid-cols-[1fr_auto] sm:items-end"><div><p className="text-lg font-semibold tracking-tight text-foreground">{brandName}</p><p className="mt-1 text-sm text-muted-foreground">{subtitle}</p><p className="mt-2 max-w-xl text-sm leading-6 text-muted-foreground">{publicIdentity.slogan[locale]}</p></div><nav aria-label={zh ? "页脚导航" : "Footer navigation"} className="flex flex-wrap gap-x-5 gap-y-2 text-sm font-medium text-muted-foreground"><Link href={path("/services")}>{zh ? "服务" : "Services"}</Link><Link href={path("/projects")}>{zh ? "项目库" : "Projects"}</Link><Link href={path("/articles")}>{zh ? "文章" : "Articles"}</Link>{zh?<Link href="/articles#wechat">公众号</Link>:null}<Link href={path("/contact")}>{zh ? "联系" : "Contact"}</Link></nav></div><div className="border-t border-hairline px-4 py-4 text-center font-mono text-[11px] uppercase tracking-[0.14em] text-muted-foreground">© {new Date().getFullYear()} {brandName} · {subtitle}</div></footer>; }
