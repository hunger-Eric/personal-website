"use client";
import Link from "next/link";
import { ArrowRight } from "lucide-react";
import { useLocale } from "@/components/LocaleProvider";
import { getPublicWebsiteProjects } from "@/config/website-projects";
const factLabels={zh:{problem:"实际要解决",solution:"系统怎样做",buyerValue:"客户为什么买",boundary:"证据边界"},en:{problem:"Business problem",solution:"How it works",buyerValue:"Why customers buy",boundary:"Evidence boundary"}} as const;
export function PublicProjectsPage(){const{locale}=useLocale();const zh=locale==="zh";const projects=getPublicWebsiteProjects(locale);const labels=factLabels[locale];return <div className="min-h-screen overflow-x-hidden bg-surface-paper pb-20 pt-28 text-surface-paper-foreground sm:pt-32"><header className="mx-auto max-w-6xl px-4"><p className="font-mono text-xs font-semibold uppercase tracking-[0.18em] text-accent">Project library</p><h1 className="mt-4 max-w-4xl text-4xl font-semibold leading-tight tracking-[-0.045em] text-foreground sm:text-6xl">{zh?"项目怎样解决真实业务问题":"How each project solves a real business problem"}</h1><p className="mt-6 max-w-3xl text-base leading-8 text-muted-foreground">{zh?"每个项目讲清楚它为谁解决什么问题、系统怎样接管流程，以及客户为什么愿意为它买单。本地测试数据只用于验证系统，不作为项目成果。":"Each project explains whose problem it solves, how the system takes over the workflow, and why a customer would pay for it. Local test data verifies the system only and is never presented as a project outcome."}</p></header><section className="mx-auto mt-12 grid max-w-6xl gap-px border border-hairline bg-hairline sm:grid-cols-2" aria-label={zh?"项目列表":"Project list"}>{projects.map((project,index)=><article key={project.id} className="flex flex-col bg-surface-paper-elevated p-6 sm:p-8"><div className="flex items-start justify-between gap-4"><span className="font-mono text-xs text-accent">0{index+1}</span><span className="max-w-[13rem] border border-hairline px-2 py-1 text-right font-mono text-[10px] uppercase tracking-[0.08em] text-muted-foreground">{project.status}</span></div><p className="mt-8 text-xs font-semibold uppercase tracking-[0.12em] text-muted-foreground">{project.category}</p><h2 className="mt-2 text-3xl font-semibold tracking-[-0.035em] text-foreground">{project.name}</h2><p className="mt-4 text-sm leading-7 text-muted-foreground">{project.summary}</p><ul className="mt-5">{project.facts.map(fact=><li key={`${fact.kind}-${fact.text}`} className="grid grid-cols-[5.25rem_minmax(0,1fr)] gap-3 border-t border-hairline py-3 text-sm leading-6"><span className="font-mono text-[10px] font-semibold uppercase tracking-[0.08em] text-accent">{labels[fact.kind]}</span><span className="text-foreground/80">{fact.text}</span></li>)}</ul><Link href={project.liveUrl ?? `/projects/${project.id}${project.interactive?"#open-geo-demo":""}`} className="mt-8 inline-flex min-h-11 items-center justify-center gap-2 self-start border border-foreground px-4 py-2 text-sm font-semibold text-foreground hover:border-accent hover:text-accent">{project.liveUrl?(zh?"了解产品":"Explore the product"):project.interactive?(zh?"参与体验":"Try the prototype"):(zh?"查看项目":"View project")}<ArrowRight className="h-4 w-4" aria-hidden/></Link></article>)}</section></div>}
